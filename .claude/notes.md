# Заметки по проекту nadin-scanner-for-small-boxes

## ⚠️ Обязательные правила при изменениях

### Версия — обновлять в ДВУХ местах одновременно
**Текущая версия: v165**

Каждый раз когда меняется версия (vXXX) — обновить ОБА места, иначе пользователь видит старую версию на странице:
1. `<title>` строка ~12 в `<head>`:
   ```html
   <title>Сканер упаковки vXXX</title>
   ```
2. `<h1>` строка ~1029 в теле страницы:
   ```html
   <h1 id="pageTitle">📦 Сканер упаковки vXXX</h1>
   ```
Быстрая проверка: `grep -n "v1[0-9][0-9]" index.html | grep "title\|pageTitle"`

---

## Архитектура проекта

- **index.html** — весь фронт (PWA, одна страница)
- **server-code.js** — Google Apps Script (GAS), деплоится отдельно как веб-приложение
- **sw.js** — Service Worker, network-first стратегия
- **manifest.json** — PWA манифест
- **BLE-SCANNER-INTEGRATION.md** — инструкция по интеграции BLE-сканера

### Два способа хостинга:
1. **GitHub Pages** — `https://artteth.github.io/nadin-scanner-for-small-boxes/` — берёт index.html напрямую, обновляется через `git push` в `main`
2. **Google Apps Script** — деплоится вручную, копипаст index.html в GAS-редактор

---

## Ключевые переменные и данные

| Переменная | Где | Описание |
|---|---|---|
| `barcodeToModel` | index.html JS | Главный кэш: штрих-код → { model, repacked, packType, type, photoUrl } |
| `TABLE_PACK_TYPES` | index.html JS | Список типов упаковки (с сервера или фолбэк) |
| `serverPackTypes` | index.html JS | Список с GAS (лист «ФОРМУЛЫ/выпадающие списки», AD, строки 3+) |
| `ble` | index.html JS | Объект BLE-сканера |
| `APPS_SCRIPT_URL` | index.html JS | URL GAS-деплоя |
| `SPREADSHEET_ID` | server-code.js | ID Google Таблицы |

---

## Частые ловушки

### renderPackButtons() — экранирование onclick
Кнопки типов упаковки генерируются динамически. При генерации `onclick`:
```javascript
// НЕПРАВИЛЬНО — JSON.stringify даёт двойные кавычки, ломает HTML-атрибут:
'onclick="selectPack(this,' + JSON.stringify(t) + ')"'

// ПРАВИЛЬНО — esc() заменяет " на &quot;:
var jsonVal = esc(JSON.stringify(t));
'onclick="selectPack(this,' + jsonVal + ')"'
```

### iOS :active не срабатывает в scroll-контейнерах
Фикс — одна строка в JS:
```javascript
document.addEventListener('touchstart', function(){}, {passive: true});
```

### Sticky thead ломается при overflow-x на предке
`.tbl-scroll` не должен иметь `overflow-x: auto` — иначе `position: sticky` на `thead th` перестаёт работать.

### openTableView() — тяжёлый рендер блокирует анимацию
Использовать `setTimeout(fn, 50)` вместо `requestAnimationFrame` — даёт CSS-анимации 50мс стартовать до блокировки потока.

---

## GAS API (server-code.js)

| action | Что делает |
|---|---|
| `getAllBarcodes` | Возвращает `{ barcodes: {...}, packTypes: [...] }` — основной запрос при старте |
| `getData` | Возвращает `{ items: [...], packTypeOptions: [...] }` |
| `saveItem` | Сохраняет repacked + packType по модели |
| `savePackaging` | Сохраняет только packType (старый метод) |
| `lookupBarcode` | Поиск одного штрих-кода |

### Листы Google Таблицы:
- **WB** — основные данные, строки с 6-й; колонки D(модель), C(тип), AQ(repacked), AS(packType)
- **Спецификация** — штрих-коды, строки с 5-й; A(модель), C(штрих-код)
- **ФОРМУЛЫ/выпадающие списки** — AD с 3-й строки = список типов упаковки
- **WB_Cards** (другая таблица) — фото; F(артикул), N(url)

---

## BLE сканер

- Работает на Android в Chrome/Edge/Brave
- На iOS — только через приложение **Bluefy**
- Ключевой момент: на iOS НЕ нажимать «Создать пару» при подключении — иначе устройство пропадает из списка
- Для отвязки: Настройки iOS → Bluetooth → ⓘ → «Забыть устройство»
- Авто-переподключение через `navigator.bluetooth.getDevices()` (без диалога)

---

## История версий (коротко)

| Версия | Что |
|---|---|
| v121 | Три режима камеры + настройки + sticky header fix |
| v122 | Типы упаковки в таблице из реальных данных GAS |
| v123 | BLE: запоминание устройства + авто-реконнект |
| v124 | BLE: фикс iOS спаренных устройств |
| v125 | BLE: инструкция про iOS пару |
| v126 | Типы упаковки из GAS (лист AD) + touch-action:manipulation |
| v127 | setTimeout вместо rAF + iOS :active хак + фото на мобиле |
| v128 | Фикс renderPackButtons: JSON.stringify + esc() |
| v129 | Кнопка ↻ + автообновление + фон. обновление после записи |
| v130 | Поле+кнопка «Найти» в строку, скрытие камер в настройках, история сканирования |
| v131 | Кнопка «Найти» шире на 20% |
| v132 | История: подтверждение очистки + миниатюра фото (fullscreen) + порядок model\|photo\|packType\|status |
| v133 | BLE для Android: расширен SERVICES, per-UUID fallback, задержка 600мс, лог диагностики в панели |
| v134 | Автофокус на поле ввода: два тогла в настройках (master + отключить при BLE) |
| v135 | manifest orientation: "any" + кнопка разблокировки ориентации в настройках |
| v136 | manifest display: "standalone" — фикс чёрной области вокруг нотча |
| v137 | manifest display: "fullscreen" возврат + html background фикс нотча |
| v138 | BLE авто-реконнект при обрыве: до 12 попыток по 5 сек, тогл в настройках |
| v139 | Кнопка принудительного переворота интерфейса 180° (body.ui-flipped) |
| v140 | Сканеры: basic→html5-qrcode, advanced→jsQR HD, ScanBot→реальный Scanbot SDK v8 RTU UI |
| v141 | «Все изделия» оптимизация для слабых устройств (2GB RAM, Android 8): chunked render rAF×50, lazy фото через IntersectionObserver, picker вместо `<select>`, debounce поиска 200мс |
| v142 | Индикатор автообновления на главной (зелёная пульсирующая точка + время последнего обновления) под кнопкой ↻ |
| v143 | Split-layout в горизонтальной ориентации (≥760px): фото изделия крупно отдельным блоком сбоку, тоглы в настройках (вкл/выкл, лево/право) |
| v144 | Bump version (исправление расхождения title/h1) |
| v145 | Фикс утечек памяти BLE (накопление gattserverdisconnected + characteristicvaluechanged слушателей, DOM reflow на каждый пакет); предзагрузка фото в дисковый кэш (fetch, не RAM) с прогрессом в настройках |
| v150 | Кэш фото через Service Worker (CacheStorage, cache-first) — фото переживают переключение «Главная ⇄ Все изделия» и перезапуск браузера; лимит 4 параллельных загрузки фото (защита слабых устройств); кнопка «Очистить кэш фото» в настройках; updateViaCache:'none' на sw.js для быстрого обновления |
| v151 | Прогресс на главном прелоадере: «бегающая» индетерминированная полоска во время JSONP-запроса, после ответа — определённый бар 0→100% + анимированный счётчик «X / Y товаров» (уникальные модели) |
| v152 | Постоянный автофокус на поле ввода: blur-listener возвращает фокус через 120 мс, touchend-listener — при тапе мимо интерактивных элементов; фокус не восстанавливается пока открыт любой оверлей |
| v153 | Фикс загрузки фото на Zebra PS20 (Android 8.1, Chrome 138): добавлен внешний .catch() в sw.js — если CacheStorage недоступна (MDM-квота, нехватка места), фото грузятся напрямую из сети вместо полного провала запроса |
| v154 | Фикс автофокуса: после закрытия/сохранения настроек фокус возвращается на поле ввода автоматически (раньше нужно было тапнуть вручную) |
| v155 | Кнопка «Заблокировать поворот» в настройках: фиксирует текущую ориентацию через screen.orientation.lock(); рядом — существующие кнопки 180° и разблокировать |
| v156 | Фикс поворота экрана в PWA: manifest.json "orientation": "any" → "portrait" — теперь системная блокировка Android работает корректно |
| v157 | Уточнение ориентации: "portrait" → "portrait-primary" — полностью блокирует и 90° и 180° автоповорот |
| v158 | Автофокус — доработка: централизованная applyFocusState() пересчитывает фокус/клавиатуру при старте, смене статуса BLE (hook в ble.updateUI) и закрытии ВСЕХ оверлеев (таблица/picker/cfg/камера/фото/BLE-панель, раньше только настройки); фикс гонки старта с BLE (клавиатура больше не мигает при автоподключении); focus({preventScroll}); фокус ставится только на stateIdle и после загрузки данных; enterkeyhint="search"; при активном BLE поле не блокируется — ручной ввод по тапу остаётся доступен |
| v159 | «Все изделия» — ускорение для слабых устройств: (1) открывается на первой вкладке-категории вместо «Все» (строк сразу мало); (2) постраничный рендер по 60 строк с догрузкой при скролле (scroll-листенер, работает на старом webview) вместо создания всех строк сразу. getTableTypes() — общий помощник для вкладок и дефолтной вкладки |
| v160 | Голосовая озвучка типа упаковки (Web Speech API): при находке уже упакованного изделия динамик произносит тип коробки/пакета; читается текст ДО первой скобки (техинфа в «(…)» не озвучивается). Тумблер + кнопка «Проверить голос» в настройках. iOS-разблокировка speechSynthesis при первом жесте (touch/click) — озвучка потом работает и по BLE/камере. lang=ru-RU + выбор русского голоса если есть. Работает в Chrome и Safari (iPhone 7 Plus / iOS 15) |
| v161 | (1) Фикс: текст в таблице «Все изделия» снова выделяется/копируется (body user-select:none переопределён для .tbl-table td/th, .tbl-model). (2) Озвучка: ползунок громкости 10–300% (по умолч. 100; API-максимум 1.0 — выше упирается в системный, честный хинт в UI); ползунок повторов 1–5 со вставкой паузы + «повторяю» + паузы между копиями (шаговый проигрыватель _runSpeechSteps через onend+setTimeout). «Проверить голос» использует текущие значения ползунков |
| v162 | Звуковые сигналы (пилик) через Web Audio перед голосом: успех — два восходящих тона, ошибка — низкий сигнал (+ голос «ничего не найдено по данному коду»), нужно выбрать упаковку — средний тон (+ голос «выберите упаковку и нажмите сохранить»). Тумблер «Звуковые сигналы» (soundCues). Web Audio разблокируется тем же жестом что и speech (unlockAudio). Громкость общая для сигналов и голоса, максимум снижен 300→100% (выше API всё равно не даёт). Кнопка теста играет сигнал+голос текущей громкости |
| v163 | Пауза между сигналом и речью: _cueDelay = длительность сигнала + CUE_GAP(320мс), чтобы между пиликом и голосом был слышимый разрыв (как пауза у «повторяю») |
| v164 | Фикс порядка «пилик → голос»: раньше речь и пилик шли по независимым таймерам и на iOS из-за задержки res() AudioContext пилик «съезжал» на речь. Теперь _emitCue сперва гарантированно resume() контекста, играет тон и по факту его окончания (+CUE_GAP) вызывает onDone → речь. Новый cueThenSpeak(kind, speakFn); success/error/select и тест переведены на колбэк-цепочку. _cueDelay удалён |
| v165 | Пилик всё ещё «съезжал» на речь (в т.ч. Safari macOS M1) из-за cold-start латентности Web Audio. Фикс: _warmAudio() — прогрев 1-сэмпловым тихим буфером перед пиликом и на каждом жесте (unlockAudio уже не once); речь запускается по РЕАЛЬНОМУ osc.onended последнего тона (+CUE_GAP), а не по clock-таймеру (с фолбэком). _tone возвращает осциллятор |

---

## v141 — оптимизация «Все изделия» для слабых устройств

**Цель:** работа на планшете с 2 ГБ ОЗУ, CPU 1.4 ГГц, Android 8.

**Узкие места до оптимизации:**
1. `<select>` в каждой строке (~10–20 опций × N строк = тысячи DOM-узлов). Нативный `<select>` особенно тяжёл в Android 8 webview.
2. Все строки рендерились одним `innerHTML` → блокировка главного потока на сотни мс.
3. Все `<img>` создавались сразу, даже с `loading="lazy"`.
4. Поиск ререндерил всю таблицу на каждый символ (`oninput="renderTableView()"`).

**Что изменено в `index.html`:**
- `renderTableView()` рендерит чанками по 50 строк через `requestAnimationFrame`. Токен `_renderToken` отменяет старый рендер, если пришёл новый запрос.
- Вместо `<select>` в строке — `<div class="tbl-pack-cell">value</div>` с CSS-стрелкой `▾`. Клик открывает общий модальный picker (один на всю таблицу).
- Фото — `<div class="tbl-thumb-lazy" data-src="...">` плейсхолдер. `IntersectionObserver` подменяет на `<img>` при попадании во viewport (rootMargin 200px).
- `tblSearchInput()` debounce 200мс перед `renderTableView()`.
- Убраны `transition: background 0.1s` и `:hover` на tr — не нужны на тач-устройствах.

**Что не тронуто:** GAS-API, формат данных, чекбокс «упаковано», логика сохранения, пагинация (её нет — всё одной таблицей, но рендер теперь не блокирующий).

**Если IntersectionObserver не поддерживается** (древний webview): фолбэк — все картинки грузятся сразу.

---

## v150 — постоянный кэш фото в Service Worker

**Проблема:** Google Drive / googleusercontent отдают фото с `Cache-Control: private, no-store` (или max-age=0). Браузерный HTTP-кэш такие ответы не сохраняет, и каждое открытие «Все изделия» или возврат с главной страницы заново тянет ВСЕ фото из сети. На слабых устройствах с медленной сетью это и есть «плохо грузятся».

**Что сделано в `sw.js`:**
- Запросы с `request.destination === 'image'` (либо URL с .jpg/.png/.webp/...) идут через стратегию **cache-first**: сначала ищем в CacheStorage (`photos-v1`), при промахе — fetch и `cache.put()` ответа.
- Кэшируются и `200 OK`, и `opaque` (cross-origin no-cors `<img>` — а у нас именно такие). CacheStorage не зависит от Cache-Control сервера.
- Все остальные запросы — старая стратегия network-first с фолбэком в кэш на оффлайне.
- В `activate` чистятся устаревшие кэши `photos-*` (если когда-то поменяется `PHOTO_CACHE`).
- Сообщение `{type:'clearPhotoCache'}` от страницы — удаляет кэш по запросу.

**В `index.html`:**
- `serviceWorker.register('sw.js', {updateViaCache: 'none'})` + `reg.update()` — иначе старая sw.js может жить в HTTP-кэше неделями.
- Очередь `_photoQueue` + лимит `PHOTO_CONCURRENCY = 4` в `_swapLazyToImg` — даже первое открытие не валит 20+ параллельных запросов на сеть/CPU слабого устройства.
- Кнопка «🗑️ Очистить кэш фото» в настройках (вызывает `caches.delete('photos-v1')` напрямую через CacheStorage API).
- Тогл «Догружать остальные фото в фоне» (бывшее «Кэшировать фото…») — теперь говорит про **дозагрузку** того, что не попало во вьюпорт.

**Важно для хостинга:**
- На **GitHub Pages** (своя origin) Service Worker работает → кэш фото живёт.
- В **Google Apps Script** (sandbox iframe на googleusercontent.com) регистрация SW тихо падает (`.catch`), кэш фото не работает — это ограничение GAS, не наша баг. Для GAS-варианта рекомендуется деплой на GitHub Pages.

---

## BLE: важно про Android Chrome (v133)

**Симптом:** сканер подключается (пикает), но штрих-коды не приходят.

**Почему отличается от iOS/Bluefy:**
- Android Chrome строго фильтрует `getPrimaryServices()` по `optionalServices` из `requestDevice()`. Если сервис не в списке — его как бы нет.
- Chrome держит блоклист для стандартных сервисов: **HID `0x1812`, Generic Access `0x1800`, Generic Attribute `0x1801`**. Их НЕЛЬЗЯ включать в `optionalServices` — иначе `SecurityError`.
- Многие сканеры по умолчанию в режиме **HID** или **SPP (Bluetooth Classic)** — Web Bluetooth их не видит вообще. Пользователь должен переключить в **BLE serial** режим служебным штрих-кодом из инструкции к сканеру.
- Между `gatt.connect()` и `getPrimaryServices()` нужна пауза ~500-600мс, иначе Android иногда возвращает пустой массив.
- Fallback: если `getPrimaryServices()` пусто — перебрать `getPrimaryService(uuid)` по списку.

**Диагностика:** кнопка «🔬 Показать лог диагностики» в панели BLE. Показывает найденные UUID сервисов/характеристик + raw hex входящих пакетов.
